# Optional: Google Drive API integration
# Install these only if you want to use direct Google Drive sync
# (Not needed if using Google Drive desktop app sync folder)

google-auth==2.29.0
google-auth-oauthlib==1.2.0
google-auth-httplib2==0.2.0
google-api-python-client==2.122.0

# To install:
# pip install -r requirements-gdrive.txt
